<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
        <title>Cuisines</title>
    </head>
    <body>
        <div class="container">
        <h1>Your chosen cuisine is: {{ cuisines.getType }}</h1>
        <p><a href="/cuisines/{{ cuisines.getTypeId }}/edit">Edit cuisines</a></p>

        {% if restaurants is not empty %}
            <p>Here are the Restaurants:</p>
            <ul>
                {% for place in restaurants %}
                    <li>{{ place.getName }}</li>
                {% endfor %}
            </ul>

            <form action='/delete_restaurants' method='post'>
                <button type='submit'>Clear restaurants</button>
            </form>

        {% endif %}

        <h4>Add a restaurant</h4>

        <form action='/restaurant_add' method='post'>
            <input id="cuisine_id" name="cuisine_id" type="hidden" value="{{ cuisines.getTypeId() }}">
            <label for='name'>Restaurant Name</label>
            <input id='name' name='name' type='text'>
            <label for='rating'>Rate it</label>
            <input type='range' name='rating' min='1' max='5'>
            <label for='review'>Review it</label>
            <textarea name='review' cols='25' rows='5'>Enter your Review here</textarea>

            <button type='submit'>Add restaurant</button>
        </form>

        <p><a href='/'>Home</a></p>
    </div>
    </body>
</html>
